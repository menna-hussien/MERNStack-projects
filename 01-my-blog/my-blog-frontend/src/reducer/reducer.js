const reducer = (state, action) => {
  //to remove a certain comment for a certain article
  /*if (action.type === 'REMOVE') {
    return { ...state, comments: comments.splice(action.payload, 1) };
  }
  //increase likes or dislikes
  if (action.type === 'INCREASE') {
    return {};
  }
*/
  return state;
};
export default reducer;
